# Copyright 2025 Forge project
#
# This file marks the resolver unit-test directory as a package so that
# pytest assigns unique module names (e.g. tests.unit.resolver.test_*)
# and avoids module import collisions with other tests that share the
# same stem. The file intentionally has no runtime side effects.


