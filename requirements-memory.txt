# Production Memory System Dependencies
# For 80% accuracy / 20% speed configuration

# Vector Database (choose one):
chromadb>=0.4.22          # Local/embedded (good for development)
qdrant-client>=1.7.0      # Cloud-ready (good for production)

# Embeddings & Re-ranking:
sentence-transformers>=2.3.1  # For embeddings and re-ranking
torch>=2.0.0,<3.0.0           # Required by sentence-transformers (CPU version)

# Optional: GPU acceleration (uncomment if you have CUDA)
# torch>=2.0.0,<3.0.0+cu118  # CUDA 11.8 version

