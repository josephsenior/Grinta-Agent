import { describe, it } from "vitest";
// This test was removed because the legacy HomeHeader component has been replaced by the new landing Header.
// No tests should import or use HomeHeader anymore.
export {};

// Legacy HomeHeader tests removed when the landing Header replaced it.
// Keep a skipped placeholder so the test runner doesn't error on an empty file.
describe.skip("Legacy HomeHeader (placeholder)", () => {
  it("placeholder", () => {
    // intentionally empty
  });
});
