// Re-export the canonical bookmarks hook from the Conversation UI module
// to avoid duplicate hook implementations and keep types consistent.
export { useConversationBookmarks } from "../conversation-bookmarks";
