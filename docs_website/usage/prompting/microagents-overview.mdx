---
title: Microagents Overview
description: Microagents are specialized prompts that enhance Forge with domain-specific knowledge. They provide expert guidance, automate common tasks, and ensure consistent practices across projects.
---

## Microagent Types

Currently Forge supports the following types of microagents:

- [General Microagents](./microagents-repo): General guidelines for Forge about the repository.
- [Keyword-Triggered Microagents](./microagents-keyword): Guidelines activated by specific keywords in prompts.

To customize Forge' behavior, create a .Forge/microagents/ directory in the root of your repository and
add `<microagent_name>.md` files inside. For repository-specific guidelines, you can ask Forge to analyze your repository and create a comprehensive `repo.md` file (see [General Microagents](./microagents-repo) for details).

<Note>
Loaded microagents take up space in the context window.
These microagents, alongside user messages, inform Forge about the task and the environment.
</Note>

Example repository structure:

```
some-repository/
└── .Forge/
    └── microagents/
        └── repo.md            # General guidelines
        └── trigger_this.md    # Microagent triggered by specific keywords
        └── trigger_that.md    # Microagent triggered by specific keywords
```

## Microagents Frontmatter Requirements

Each microagent file may include frontmatter that provides additional information. In some cases, this frontmatter
is required:

| Microagent Type                 | Required |
|---------------------------------|----------|
| `General Microagents`           | No       |
| `Keyword-Triggered Microagents` | Yes      |
