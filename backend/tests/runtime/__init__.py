"""Runtime tests for `LocalRuntime`.

Non-local runtime implementations (Docker/Remote/Kubernetes) were removed
from this branch; tests have been adapted to use `LocalRuntime` where
appropriate and Docker-specific integration tests were deleted.
"""
